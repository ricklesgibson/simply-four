<!-- Google Maps view -->
 <!DOCTYPE html>
<html>
  <head>

  <!-- seeing if this is getting ignored  

  Now is it? -->
    <script type="text/javascript"
      src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCnKu_jgX1HO78RP-td7d9uvup_TkmJg2k">
    </script>
    <script type="text/javascript">
      //function initialize() {
        //Plain View
        // var myLatlng = new google.maps.LatLng(-25.363882,131.044922);
        // var mapOptions = {
        //   center: { lat: 34.012982, lng: -118.495186},
        //   zoom: 20
        // };
        // var map = new google.maps.Map(document.getElementById('map-canvas'),
        //     mapOptions);

        // Marker view
        // var myLatlng = new google.maps.LatLng(34.012982,-118.495186);
        // var mapOptions = {
        //   zoom: 20,
        //   center: myLatlng
        // }
        // var map = new google.maps.Map(document.getElementById("map-canvas"), mapOptions);

        // // To add the marker to the map, use the 'map' property
        // var marker = new google.maps.Marker({
        // position: myLatlng,
        // map: map,
        // title:"General Assembly!"
        // });

        //Add marker directly

        // var myLatlng = new google.maps.LatLng(34.012982,-118.495186);
        // var mapOptions = {
        //   zoom: 20,
        //   center: myLatlng
        // }
        // var map = new google.maps.Map(document.getElementById("map-canvas"), mapOptions);

        // var marker = new google.maps.Marker({
        //     position: myLatlng,
        //     title:"General Assembly!"
        // });

        // // To add the marker to the map, call setMap();
        // marker.setMap(map);

      // }
      // google.maps.event.addDomListener(window, 'load', initialize);

      // Animate a marker
      // The following example creates a marker in SaMo, CA
      // using a DROP animation. Clicking on the marker will toggle
      // the animation between a BOUNCE animation and no animation.

      var saMo = new google.maps.LatLng(34.012982, -118.495186);
      var gA = new google.maps.LatLng(34.012982, -118.495186);
      var marker;
      var map;
      //var image = {url: 'https://generalassemb.ly/online/assets/ga-lockup-logo-9d95d5d0a1c966540edaedcb440b1963.png', size: new google.maps.Size(200, 302)}
      var infowindow = new google.maps.InfoWindow();
      var i = 0;

      function initialize() {
        var mapOptions = {
          zoom: 20,
          center: saMo
        };

        map = new google.maps.Map(document.getElementById('map-canvas'),
                mapOptions);

        marker = new google.maps.Marker({
          map:map,
          //draggable:true,
          //animation: google.maps.Animation.DROP,
          position: gA,
          title:"General Assembly!",
          //icon: image,


        });
        //google.maps.event.addListener(marker, 'click', toggleBounce);

        //this bit adds the info window
        google.maps.event.addListener(marker, 'click', (function(marker, i) {
               return function() {
                 infowindow.setContent("General Assembly!");
                 infowindow.open(map, marker);
               }
             })(marker, i));
      }
      //Animatin toggle
      // function toggleBounce() {

      //   if (marker.getAnimation() != null) {
      //     marker.setAnimation(null);
      //   } else {
      //     marker.setAnimation(google.maps.Animation.BOUNCE);
      //   }
      // }

      google.maps.event.addDomListener(window, 'load', initialize);





    </script>
  </head>
  <body>
<div id="map-canvas"></div>
  </body>
</html>