 <nav class="navbar navbar-default navbar-inverse navbar-static-top" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">DataLus</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        

        <!-- this is sloppy and the whole navbar and styling needs to be redone -->
          <% if current_user != nil %>
            <li class="active"> <%#= link_to "Home", show_user_path %></li>
            <li class="active"> <%= link_to "Log out", sessions_path, method: :delete %></li>
            <li class="active"> <%= link_to "Edit Page", edit_user_path(current_user)%></li>
            <li class="active"> <%= image_tag @user.avatar.url(:icon) %> </li>
           <li class="active"> <%= link_to "#{@user.first_name}", "#" %></li>

          <% else %>
            <li class="active"> <%= link_to "Sign up", new_user_path, method: :get %></li>
            <li class="active"> <%= link_to "Log in", new_sessions_path %></li>
          <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
  </nav>

  